---
/**
 * Página de Error 404 (Recurso no encontrado)
 * =========================================
 * Se muestra cuando un usuario intenta acceder a una ruta inexistente.
 * Mantiene la consistencia visual del sitio y ofrece un camino de retorno al inicio.
 */
import Layout from '@/layouts/Layout.astro';
---

<Layout title="Página no encontrada" booleanLayout={false}>
	<!-- Contenedor principal centrado para el mensaje de error -->
	<div
		class="flex min-h-[calc(100vh-200px)] flex-col items-center justify-center bg-gray-50 px-4 text-center"
	>
		<!-- Código de error con efecto de opacidad -->
		<h1 class="font-secondary-font text-secondary-800 mb-4 text-9xl font-bold opacity-20">404</h1>
		<h2 class="mb-6 text-3xl font-bold text-gray-800 md:text-4xl">¡Ups! Página no encontrada</h2>
		<p class="mb-4 max-w-md text-lg text-gray-600">
			Lo sentimos, la página que estás buscando no existe o ha sido movida.
		</p>
		<p class="mb-8 max-w-md text-gray-500">Tal vez quieras volver al inicio o verificar la URL.</p>
		<a
			href="/"
			class="bg-secondary-700 hover:bg-secondary-800 focus:ring-secondary-300 rounded-full px-8 py-3 text-lg font-semibold text-white transition-colors focus:ring-4 focus:outline-none"
		>
			Volver al Inicio
		</a>
	</div>
</Layout>
