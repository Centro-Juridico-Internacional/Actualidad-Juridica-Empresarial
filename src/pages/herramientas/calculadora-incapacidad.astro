---
/**
 * Herramienta: Calculadora de Incapacidad Médica
 * ============================================
 * Calcula los porcentajes de pago por enfermedad general (EPS)
 * y accidente de trabajo (ARL) según la distribución empresa-seguridad social.
 */
import Layout from '@/layouts/Layout.astro';
import Breadcrumb from '@/components/ui/Breadcrumb.astro';
import IncapacidadCalculator from '@/components/features/tools/IncapacidadCalculator.tsx';
import PageHero from '@/components/ui/PageHero.astro';

// URL absoluta para el generador de esquemas
const { href } = Astro.url;

/**
 * Schema JSON-LD - HealthApplication
 * Define la funcionalidad médica/administrativa de la herramienta ante Google.
 */
const schema = {
	'@context': 'https://schema.org',
	'@type': 'WebApplication',
	name: 'Calculadora de Incapacidad Médica',
	operatingSystem: 'All',
	applicationCategory: 'HealthApplication',
	url: href,
	description:
		'Calculadora oficial de incapacidad médica en Colombia: enfermedad general, accidente laboral, EPS y ARL.',
	provider: {
		'@type': 'Organization',
		name: 'Centro Jurídico Internacional'
	},
	offers: {
		'@type': 'Offer',
		price: '0',
		priceCurrency: 'COP'
	}
};
---

<script type="application/ld+json" set:html={JSON.stringify(schema)} />

<Layout title="Incapacidad Médica" booleanLayout={true} breadcrumbTitle="Herramientas">
	<PageHero
		title="Calculadora de Incapacidad Médica"
		subtitle="Conoce cuánto te deben pagar por incapacidad según EPS o ARL, de acuerdo con la normativa colombiana."
	/>

	<section class="container-main py-12">
		<Breadcrumb title="Herramientas" className="mb-8" />

		<div class="flex flex-col items-center">
			<IncapacidadCalculator client:load />

			<div class="prose prose-secondary mt-12 max-w-3xl rounded-2xl bg-gray-50 p-8 text-gray-700">
				<h3>¿Cómo funciona esta herramienta?</h3>
				<p>Incluye cálculos oficiales para:</p>
				<ul>
					<li>
						<strong>Enfermedad general:</strong> días 1 y 2 paga empresa, desde el 3 paga EPS al 66.67%
					</li>
					<li><strong>Accidente de trabajo:</strong> ARL paga el 100% desde el primer día</li>
					<li>
						<strong>Hospitalización:</strong> Se liquida como incapacidad por enfermedad general
					</li>
				</ul>
				<p>Los cálculos son aproximados según la normativa vigente.</p>
			</div>
		</div>
	</section>
</Layout>
