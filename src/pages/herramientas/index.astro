---
import Layout from "@/layouts/Layout.astro";
import Breadcrumb from "@/components/ui/Breadcrumb.astro";

const tools = [
  {
    title: "Calculadora de Liquidación Laboral",
    slug: "calculadora-laboral",
    icon: "💼",
    desc: "Calcula cesantías, intereses, prima, vacaciones y total de liquidación según normativa del Ministerio."
  },
  {
    title: "Calculadora de Indemnización por Despido",
    slug: "calculadora-despido",
    icon: "⚖️",
    desc: "Simula cuánto debe pagar una empresa por un despido sin justa causa según tipo de contrato."
  },
  {
    title: "Calculadora de Horas Extras y Recargos",
    slug: "calculadora-horas-extras",
    icon: "⏱️",
    desc: "Determina el valor de horas extra diurnas, nocturnas, dominicales y festivas."
  },
  {
    title: "Calculadora de Incapacidad Médica",
    slug: "calculadora-incapacidad",
    icon: "🏥",
    desc: "Calcula pagos por incapacidad de enfermedad general, accidente laboral y EPS/ARL."
  },
  {
    title: "Calculadora de Retención en la Fuente",
    slug: "calculadora-retencion",
    icon: "📊",
    desc: "Proyección mensual de retención aplicando UVT, ingresos, deducciones y rentas exentas."
  },
  {
    title: "Calculadora Contrato de Aprendizaje",
    slug: "calculadora-contrato-aprendizaje",
    icon: "🎓",
    desc: "Calcula auxilio de sostenimiento e indemnización por terminación anticipada."
  }
];

const { href } = Astro.url;

const schema = {
  "@context": "https://schema.org",
  "@type": "ItemList",
  itemListElement: tools.map((tool, index) => ({
    "@type": "ListItem",
    position: index + 1,
    name: tool.title,
    url: `${href}/${tool.slug}`
  }))
};
---

<script type="application/ld+json" set:html={JSON.stringify(schema)} />

<Layout title="Herramientas Jurídicas" booleanLayout={true} breadcrumbTitle="Herramientas">
  
  <!-- Hero -->
  <section class="relative flex h-[25vh] min-h-[220px] items-center justify-center overflow-hidden bg-green-900">
    <div class="absolute inset-0 opacity-20">
      <svg class="h-full w-full" width="100%" height="100%" viewBox="0 0 800 800">
        <defs>
          <pattern id="dotPattern" x="0" y="0" width="20" height="20" patternUnits="userSpaceOnUse">
            <circle cx="2" cy="2" r="1" fill="white" />
          </pattern>
        </defs>
        <rect width="800" height="800" fill="url(#dotPattern)" />
      </svg>
    </div>

    <div class="container-main relative z-10 text-center">
      <h1 class="mb-3 text-4xl font-bold text-white">Herramientas Jurídicas</h1>
      <p class="text-green-100 text-lg">Accede a nuestras herramientas legales interactivas y gratuitas</p>
    </div>
  </section>

  <section class="container-main py-12 animate-fadeIn">
    <Breadcrumb title="Herramientas" />

    <div class="mt-8 space-y-6">
      {tools.map(tool => (
        <a
          href={`/herramientas/${tool.slug}`}
          class="group flex items-start gap-6 rounded-2xl bg-white p-6 shadow-md border border-gray-100 transition-all hover:-translate-y-1 hover:shadow-xl"
        >
          <div class="text-4xl transition-transform group-hover:scale-110">{tool.icon}</div>

          <div class="flex-1">
            <h2 class="text-xl font-bold text-gray-900 group-hover:text-green-700 transition-colors">
              {tool.title}
            </h2>
            <p class="text-gray-600 mt-1">{tool.desc}</p>
          </div>

          <span class="text-green-700 font-semibold opacity-0 group-hover:opacity-100 transition-all">
            Usar →
          </span>
        </a>
      ))}
    </div>
  </section>
</Layout>
